suppressMessages(library(scipiper))
email_to_use <- "lcarr.usgs@gmail.com"
options(
  gargle_oauth_cache = ".secrets",
  gargle_oauth_email = email_to_use
)
message(sprintf('Loaded `scipiper` and set Google Drive authentication configs for %s. See `.Rprofile` to make updates.', email_to_use))

